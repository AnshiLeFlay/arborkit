# @arborkit/postgres

Alpha PostgreSQL persistence and pgvector search for ArborKit. Call `store.migrate()` explicitly.

The package supports multiple artifacts in one schema and protects writers with storage-level compare-and-set.
`1.6.0-alpha` schemas and APIs may change between alpha releases. Back up important data.
