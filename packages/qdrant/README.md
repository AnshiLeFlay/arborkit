# @arborkit/qdrant

Alpha Qdrant `VectorIndexPort` for ArborKit. Qdrant is a rebuildable semantic index; keep the authoritative artifact in SQLite or PostgreSQL.

Call `ensureCollection()` explicitly before opening a durable session. `1.6.0-alpha` APIs may change between alpha releases.
